<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_CAD"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        xmlns:ViewModes="clr-namespace:WPF_CAD.ViewModes" x:Class="WPF_CAD.MainWindow"
        mc:Ignorable="d"
        Title="{Binding Title}" Height="1080" Width="1920" WindowStartupLocation="CenterScreen" WindowState="Maximized">
    <Window.DataContext>
        <ViewModes:MainWindowViewMode/>
    </Window.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="8*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <Menu>
                <MenuItem Header="File">
                    <MenuItem Header="New" Command="{Binding NewFileCommand}"/>
                    <MenuItem Header="Open" Command="{Binding OpenFileCommand}"/>
                    <MenuItem Header="Save" Command="{Binding SaveFileCommand}"/>
                    <MenuItem Header="Save As" Command="{Binding SaveAsFileCommand}"/>
                    <MenuItem Header="Save as Plt" Command="{Binding SaveFileAsPltCommand}"/>
                </MenuItem>

                <MenuItem Header="HardWare Setup" Command="{Binding HardWareSetupCommand}"/>
            </Menu>
        </Grid>

        <Grid Grid.Column="1">

        </Grid>
    </Grid>
</Window>
